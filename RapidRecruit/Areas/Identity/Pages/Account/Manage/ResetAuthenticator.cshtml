@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Resetuj ključ autentifikatora";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Ako resetujete ključ autentifikatora, vaša autentifikator aplikacija neće raditi dok je ponovo ne konfigurirate.</strong>
    </p>
    <p>
        Ovaj proces onemogućava 2FA dok ne verifikujete vašu autentifikator aplikaciju.
        Ako ne završite konfiguraciju autentifikator aplikacije možete izgubiti pristup vašem nalogu.
    </p>
</div>
<div>
    <form id="reset-authenticator-form" method="post">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit">Resetuj ključ autentifikatora</button>
    </form>
</div>
