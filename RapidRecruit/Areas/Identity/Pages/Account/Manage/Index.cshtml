@page
@model IndexModel
@{
    ViewData["Title"] = "Profil";
    ViewData["ActivePage"] = ManageNavPages.Index;
}
<div class="space-y-6">
    <div>
        <h2 class="text-2xl font-bold text-gray-900">Podešavanja Profila</h2>
        <p class="mt-1 text-sm text-gray-600">Ažurirajte podešavanja naloga i kontakt informacije.</p>
    </div>
    <partial name="_StatusMessage" for="StatusMessage" />
    <div class="bg-white shadow rounded-lg">
        <form id="profile-form" method="post" class="space-y-6 p-6">
            <div asp-validation-summary="ModelOnly"
                 class="mb-4 p-4 rounded-md bg-red-50 text-sm text-red-600"
                 role="alert">
            </div>
            <div class="space-y-1">
                <label asp-for="Username" class="block text-sm font-medium text-gray-700">Korisničko Ime</label>
                <div class="mt-1">
                    <input asp-for="Username"
                           disabled
                           class="block w-full px-3 py-2 bg-gray-100 border border-gray-300 rounded-md shadow-sm text-gray-500 sm:text-sm"
                           placeholder="Molimo izaberite korisničko ime." />
                </div>
            </div>
            @if (User.Claims.Any(c => c.Type == "AccountType" && c.Value == "Business"))
            {
                <div class="space-y-1">
                    <label asp-for="Input.BusinessName" class="block text-sm font-medium text-gray-700">Naziv Firme</label>
                    <div class="mt-1">
                        <input asp-for="Input.BusinessName"
                               required
                               class="block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm"
                               placeholder="Unesite naziv vaše firme." />
                    </div>
                    <span asp-validation-for="Input.BusinessName" class="mt-1 text-sm text-red-600"></span>
                </div>
            }
            <div class="grid grid-cols-1 gap-6 sm:grid-cols-2">
                <div class="space-y-1">
                    <label asp-for="Input.FirstName" class="block text-sm font-medium text-gray-700">Ime</label>
                    <div class="mt-1">
                        <input asp-for="Input.FirstName"
                               required
                               class="block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm"
                               placeholder="Unesite vaše ime" />
                    </div>
                    <span asp-validation-for="Input.FirstName" class="mt-1 text-sm text-red-600"></span>
                </div>
                <div class="space-y-1">
                    <label asp-for="Input.LastName" class="block text-sm font-medium text-gray-700">Prezime</label>
                    <div class="mt-1">
                        <input asp-for="Input.LastName"
                               required
                               class="block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm"
                               placeholder="Unesite vaše prezime" />
                    </div>
                    <span asp-validation-for="Input.LastName" class="mt-1 text-sm text-red-600"></span>
                </div>
            </div>
            <div class="space-y-1">
                <label asp-for="Input.PhoneNumber" class="block text-sm font-medium text-gray-700">Broj Telefona</label>
                <div class="mt-1">
                    <input asp-for="Input.PhoneNumber"
                           class="block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm"
                           placeholder="Unesite vaš broj telefona." />
                </div>
                <span asp-validation-for="Input.PhoneNumber" class="mt-1 text-sm text-red-600"></span>
            </div>
            <div class="flex justify-end pt-4">
                <button id="update-profile-button"
                        type="submit"
                        class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                    Sačuvaj Promene
                </button>
            </div>
        </form>
    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}